spring:
  application:
    name: notificaciones-service
  datasource:
    url: jdbc:h2:mem:notificaciones_db
    driver-class-name: org.h2.Driver
    username: sa
    password: 
  h2:
    console:
      enabled: true
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
flyway:
  enabled: true
server:
  port: 8087
